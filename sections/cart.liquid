<div
  class="@container/section space-y-lh"
  data-cart-lines
  data-section-id="{{ section.id }}"
>
  {%- if cart.empty? -%}
    {% content_for 'block', type: '_cart-header-empty', id: 'cart-empty' %}
  {%- else -%}
    {% content_for 'block', type: '_cart-header', id: 'cart-content' %}
  {%- endif -%}

  {%- render 'cart' -%}
</div>

{% schema %}
{
  "name": "Cart",
  "tag": "section",
  "class": "section",
  "settings": [],
  "blocks": [
    { "type": "_cart-header" },
    { "type": "_cart-header-empty" }
  ]
}
{% endschema %}
